package interview;

import java.util.*;

public class generic_hash {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		HashMap <Integer,Integer> map=new HashMap<>();
		
		
		
//		map.put(k, v);
//		map.putIfAbsent(k, v);
//		map.containsKey(v);
//		map.containsValue(v);
//		map.replace(k, v);
//		map.get(k);//returns value associated with key k
//		map.remove(k);//removes value associated with key k
//		map.remove(k,v);
//		map.keySet();//returns set of keys
//		map.values();//returns set of values
//		map.size();
//		map.forEach((key,value) -> {
//			if((int)value>a.length/2) {
//				System.out.println("key: "+key);
//			}
//		});
	}

}
